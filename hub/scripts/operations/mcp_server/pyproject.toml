[project]
name = "inas-operations-mcp"
version = "0.1.0"
description = "Read-only stdio MCP adapter for INA Hub Operations"
requires-python = ">=3.11"
dependencies = ["mcp>=1.28,<2"]

[dependency-groups]
dev = ["ruff>=0.9.6"]

[tool.uv]
package = false
