# The Gateway host may open outbound WAN connections, but it is not a router for
# the device AP. nftables enforces the same boundary independently.
net.ipv4.ip_forward=0
net.ipv6.conf.all.forwarding=0
