# Keep the Gateway clock synchronized from the WAN using the distribution's
# normal chrony sources, and serve that time only to the isolated device LAN.
allow 192.168.50.0/24
bindacqaddress 0.0.0.0
local stratum 10 orphan
